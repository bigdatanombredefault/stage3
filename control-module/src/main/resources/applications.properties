# Server Configuration
server.port=7000

# Service URLs
service.ingestion.url=http://localhost:7001
service.indexing.url=http://localhost:7002
service.search.url=http://localhost:7003

# HTTP Client Configuration
http.connect.timeout=10000
http.read.timeout=60000

# Pipeline Configuration
pipeline.check.interval=2000
pipeline.max.retries=30

# Logging
log.level=INFO