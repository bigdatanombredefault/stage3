# Server Configuration
server.port=7002

# --- NEW: ActiveMQ Configuration ---
# This MUST match the configuration in the ingestion-service
activemq.broker.url=tcp://localhost:61616
activemq.queue.name=document.ingested

# Datalake Configuration (still needed by DatalakeReader)
datalake.path=../datalake

# Logging
log.level=INFO